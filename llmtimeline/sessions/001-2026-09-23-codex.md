# Session 001 · 2026-09-23 · codex

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Architecture and code trace
status: done
intent: understand the implemented protocol end to end and compare it with repository documentation.
did: read Rust contracts, TypeScript SDK, signer/aggregator/relayer services, deployment scripts, Docker configuration, and tests. Confirmed the envelope-first architecture and split of authority and persistence.
files: llmtimeline/state.md, llmtimeline/sessions/001-2026-09-23-codex.md
decisions: leave implementation files unchanged because the user requested understanding, not a code change.
gotchas: envelope verification does not itself check on-chain governance pause or independently recompute the policy hash; the attestation contract checks policy hash and pause during cell creation. Coordinator intentionally fails a collection when any fulfilled signer response conflicts, even if other signers could form quorum.
next: use the final walkthrough as the project map; test live-chain behavior before treating the protocol as deployment ready.

## Verification
status: done
intent: establish which checks pass in this workspace.
did: TypeScript workspace build passed. Rebuilt better-sqlite3 with installed Node 22 after initial Node 26 ABI failure; 12 JavaScript tests passed. Rust unit tests passed with host target (2 golden vectors); RISC-V release build passed with the specified cross compiler.
files: ignored node_modules/ and target/ generated build output; llmtimeline/state.md, llmtimeline/sessions/001-2026-09-23-codex.md
gotchas: Cargo's default target is RISC-V, so plain `cargo test --workspace` cannot run Rust's host test harness. Initial npm test used Node 26, which is incompatible with the existing SQLite native addon. Neither suite executes real CKB transactions.
next: run a devnet transaction integration flow when validating deployment behavior.
