{
  "name": "threshold-price-oracle-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/price-trigger-ts"
  ],
  "scripts": {
    "build": "npm run build -w lean-oracle-sdk && npm run build -w lean-oracle-publisher && npm run build -w lean-oracle-mirror && npm run build -w lean-oracle-watchdog",
    "test": "npm test -w lean-oracle-sdk && npm test -w lean-oracle-publisher && npm test -w lean-oracle-mirror && npm test -w lean-oracle-watchdog"
  },
  "engines": {
    "node": ">=22"
  }
}
