# Session 008 · 2026-09-26 · opus

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Architecture review and gstack adoption plan
status: done
intent: redo session 007's unfinished review: read the whole architecture, verify tests and the live testnet, and map a gstack workflow onto the project (user: pre-release, no backward-compatibility constraint).
did: read docs/oracle-design.md, both contracts, lean-oracle-common, publisher node/store/main, mirror ingest/committee/store, SDK client, CI workflows, testnet ops doc. Ran cargo test --workspace (23 pass) and npm test (SDK 18, publisher 31, mirror 4, watchdog 2). Read-only checks on the testnet: mirror /health fresh, 601/600 BTC/USDT updates in 10 min with no gaps; on the droplet (ssh, read-only) volumes after ~22 h: mirror 786 MB, majors 375 MB, ckb 40 MB; 15 GB free; load avg ~2 on 1 vCPU. Wrote docs/review/2026-09-26-architecture-and-gstack-plan.md.
files: docs/review/2026-09-26-architecture-and-gstack-plan.md (new, uncommitted); llmtimeline/state.md; llmtimeline/sessions/008-2026-09-26-opus.md
decisions: ran the gstack router preamble only; did not run the interactive review skills (office-hours, plan-*-review, cso), because their output depends on the user's answers. The review doc lists them as next steps in order.
gotchas: P0: testnet storage grows ~1.3 GB/day with no retention (design doc says 50 MB/day, ~8x off); the watchdog has no disk check. P1 before mainnet (immutable contracts): rotation erases on-chain usable history (current-set-only); no usable pause (only via full rotation with all-key PoP + set_index bump; no CLI sets the flag); committee-cell lock can veto rotations; per-feed signature re-verification cost; TPOU/TPOB naming. publisher_set_type has zero ckb-testtool tests and the devnet e2e never runs in CI. A backup container (alpine sqlite .backup + gzip) was running at 12:03 UTC, not at the 03:30 cron time; not investigated.
next: user picks from section 5 of the review doc. First: retention + disk alert PR; then CLAUDE.md/backlog/versioning/PR-flow setup for gstack; then /office-hours → /plan-ceo-review → /plan-eng-review (+ /codex) on docs/oracle-design.md to produce a v1 contract freeze list.

## gstack /office-hours: v1 contract freeze design
status: done
intent: run the user's first gstack skill (office-hours, startup/ecosystem mode) to do the skipped product-thinking phase and decide what the immutable contracts must support.
did: demand diagnostic (pre-product; only real consumer is the team's up/down pools, not live; lending with CKB collateral is the reasoned second case). Status quo per user: 0.x Pyth relay costs ~$800+/month, has possible legal restrictions, and has no CKB pairs. Web research (settlement manipulation paper: single-instant settlement is pushable, so use windowed prices). Codex cold second opinion (challenged "clear edge"; proposed a 1,000-round settlement benchmark). User chose approach B (full v1 contract freeze). Spec review: 3 rounds, 8/10, 16/19 findings fixed, 3 recorded. User reframed: Lean Oracle is open, general-purpose infrastructure; pools and lending are reference use cases only. Doc APPROVED.
files: docs/designs/v1-contract-freeze.md (new, uncommitted; also ~/.gstack/projects/calledAdo-lean-oracle-v2/adokiye-main-design-20260926-140837.md with review rounds)
decisions: approach B freeze items: (1) rotation-safe history (previous set + first tick) + minimum rotation interval via relative `since`; (2) quorum pause, exempt from the interval, as the emergency lever; (3) committee cell lock that cannot veto rotations; (4) one signature check per tx for many feed cells; (5) USDT/USD in the same committee as CKB pairs; (6) optional TWAP60 windowed feeds at boundary ticks, 45/60 minimum, pools settle on publish_time_ms == boundary*1000; (7) rename TPOU/TPOB. Mainnet needs n >= 4 publishers.
gotchas: open reviewer concerns: pause/unpause spends the committee cell and so restarts the `since` rotation clock; pools behavior during a pause is undefined; two Open Questions lines are stale versus items 2 and 3.
next: user's assignment: write the pools round parameters (round length, boundary schedule, settlement deadline, stake per round) into the doc; then /plan-eng-review on docs/designs/v1-contract-freeze.md.

## Correction: oracle parameters are oracle-side
status: done
intent: user objected that the assignment asked for up/down pools numbers while we are building Lean Oracle.
did: removed the pools-parameter prerequisite and assignment from docs/designs/v1-contract-freeze.md; window length, boundary schedule and rotation interval are now oracle defaults decided in /plan-eng-review; pools remains only an example use case.
decisions: no Lean Oracle decision depends on a single consumer application.
next: /plan-eng-review on docs/designs/v1-contract-freeze.md.

## gstack /plan-eng-review on the v1 contract freeze
status: done
intent: lock the engineering plan for the immutable-contract freeze before any code.
did: scope gate (D1 defer TWAP60, D2 defer committee layout, D3 keep arrangement); architecture D4-D9, code quality D10, tests D11-D12 (33 planned paths, 6 tested today), performance D13; Codex outside voice found 5 real issues (interval backdating via absolute since, cross-committee governance replay (testnet majors/ckb have identical state), stored prices outliving pause/revoke, quorum-compromise model, leader rule partition), all applied as D14-D18; backlog D19 (TWAP60), D20 (user's idea: move CKB pairs into majors, retire ckb committee). Wrote ledger, diagrams, failure modes, lanes, tasks T1-T11 and the GSTACK REVIEW REPORT into docs/designs/v1-contract-freeze.md; test plan + tasks JSONL in ~/.gstack/projects/calledAdo-lean-oracle-v2/.
files: docs/designs/v1-contract-freeze.md; llmtimeline/state.md; ~/.gstack/projects/calledAdo-lean-oracle-v2/{adokiye-main-eng-review-test-plan-20260926-164242.md,tasks-eng-review-20260926-164242.jsonl}
decisions: committee data v2 with per-committee min_rotation_interval_s (default 24 h) enforced by relative since (D14 supersedes D5); previous set kept with previous_until_ms, revoke flag on rotation (D4) and OP_REVOKE_PREVIOUS any time (D17); revoking rotations skip the interval (D9); OP_PAUSE/OP_UNPAUSE by quorum (D8); governance hashes bind the committee type hash (D15); always-success committee lock + guards (D7); price_feed_type leader/follower, one header per committee per tx, shared blob in leader witness (D6, D13, D18); feed cell gets set_index (125 -> 129 bytes) and the consumer helper checks the committee cell (D16); magics LOPU/LOOB; regression contract with <= 25.3M cycles single-feed at 3-of-4 (D11); devnet E2E in CI (D12).
gotchas: nothing implemented yet; all work is uncommitted on main. Start implementation on a branch.
next: T1 in contracts/common (see Implementation Tasks in the doc).

## T1: contracts/common v2 formats
status: done
intent: implement task T1 of docs/designs/v1-contract-freeze.md on branch freeze/v1-contracts.
did: rewrote publisher_set.rs (v2 layout, PreviousSet, ops 1-5, check_transition, set_for, trusts, needs_pop, needs_interval, committee-bound update_hash/pop_hash); price_update.rs (magic LOPU, verify_header + find_entry, previous-set acceptance); price_feed.rs (set_index, 129 bytes); consumer.rs (committee arg, Paused/UntrustedSet); errors.rs (+ERROR_PUBLISHER_SET_INTERVAL 65, ERROR_PUBLISHER_SET_CELL 66, ERROR_FEED_LEADER 92); protocol_hash.rs set domains -> V2. cargo test -p lean-oracle-common: 9 pass.
files: contracts/common/src/*.rs (commit after 1da8d2d)
decisions: op codes ROTATE=1, ROTATE_REVOKE=2, PAUSE=3, UNPAUSE=4, REVOKE_PREVIOUS=5; rotate keeps the pause flag; ROTATE requires previous_until_ms strictly increasing.
gotchas: branch does not build as a whole: publisher_set_type (update_hash/pop_hash signatures), examples/price_trigger_lock (check_feed_cell arity), and tests/ crate need T2/T3/T4; vectors not regenerated (T5); SDK untouched (T6).
next: T2 contracts/publisher_set_type/src/main.rs (ops, relative since via load_input_since >= min_rotation_interval_s for OP_ROTATE only, always-success guards: exactly one continuing output, capacity not lowered), then T3 price_feed_type leader/follower.

## T2-T4 (+ Rust side of T5): contracts and Rust tests
status: done (Rust); TypeScript side pending
intent: implement publisher_set_type ops, price_feed_type leader/follower, port and extend Rust tests.
did: publisher_set_type rewritten (check_transition, committee-bound auth/PoP via load_script_hash, relative-timestamp since check for OP_ROTATE, lock/capacity guards). price_feed_type: find_leader (lowest-index input with same code_hash/hash_type, same committee in data, continuing output), blob from leader's witness, verify_header only by the leader, find_entry for every cell. price_trigger_lock loads the committee cell dep (errors 111-113). tests: fixtures v2, new publisher_set_type_tests.rs (7), 6 new price_feed_type tests, lock test for pause/untrusted set, vectors.rs emits v2 + governance hashes; vectors/protocol.json regenerated. cargo test --workspace: 42 pass. Cycles: single feed 3-of-4 22.56M (was 24.1M), 7-of-9 52.5M (was 55.9M), 1/2/3 feeds 22.58/22.71/22.85M; rotation 4 keys 52.5M, 9 keys 119.9M.
files: contracts/{publisher_set_type,price_feed_type}/src/main.rs, examples/price_trigger_lock/src/main.rs, tests/src/*, vectors/protocol.json (commit 9617a17)
decisions: followers ignore their own witness and read only the leader's blob, so a second update in the same tx fails with ERROR_UPDATE_FEED_NOT_FOUND (D18 by construction).
gotchas: contracts/checksums.txt is stale: scripts/build-contracts.sh --update crashes under QEMU on this Mac; enable Docker Desktop "Use Rosetta for x86_64/amd64 emulation" and rerun, or CI will fail its checksum step. npm test now fails on SDK vector tests until T6 (SDK v2 codecs). Contract docs/spec (T10) not yet updated.
next: T6 packages/sdk: protocol/publisherSet.ts v2 (min interval, previous), governance hashes with committee type hash, ops 1-5, priceUpdate magic LOPU, observation magic LOOB, priceFeed set_index (129 bytes), tx builders (pause/unpause/revoke, multi-feed with shared leader blob); then publisher T7, mirror T8.

## Reproducible contract checksums
status: done
did: user installed Rosetta (softwareupdate --install-rosetta) and enabled Docker Desktop "Use Rosetta"; Docker's VM also registers a QEMU x86_64 binfmt handler that wins, so the build was run with it temporarily disabled (privileged alpine container: echo 0 > /proc/sys/fs/binfmt_misc/x86_64) and re-enabled after. Earlier failures also coincided with a lost internet connection (rustup downloads rust-std in each fresh container). scripts/build-contracts.sh --update succeeded; a second verify build matched; 42 Rust tests pass.
files: contracts/checksums.txt
gotchas: on this Mac, to run scripts/build-contracts.sh: need network, and disable the QEMU x86_64 handler in Docker's VM for the run (it resets when Docker restarts); otherwise rustc segfaults under QEMU.
next: T6 SDK.

## T6-T8 + deploy port + consensus bug fix
status: done
intent: bring SDK, publisher, mirror and deploy tool onto the v2 formats so the whole branch is green.
did: SDK (ec1689b): PublisherSet v2, ops, transitionError, committee-bound hashes, setFor/trustsSet, LOPU/LOOB, feed setIndex + isFeedPriceTrusted, signGovernance, governCommittee (relative since for OP_ROTATE), updateFeedCells (shared leader blob), client.updateFeedCells/isFeedPriceTrusted; 20 tests. Publisher (475f7e1): governance CLI next-set --op/--until-ms, sign-governance, governance-status, --committee; 32 tests. Mirror: previous set + untilMs from chain, learns previous after restart, revoked set stops ingestion; 4 tests. Deploy: minRotationIntervalS (default 86400), govern:committee --op; e2e files ported (not run). Bug (ef83a88): node.ts backup skipped tick_request when it held all observations and proposed a conflicting header, losing the tick; fixed per design section 5. Full CI-equivalent run: npm run build (+deploy), npm test all green, publint ok, cargo test 42 ok, apps/web tsc ok.
files: packages/sdk/**, apps/publisher/**, apps/mirror/**, apps/deploy/**
decisions: no backward-compat aliases (signRotation, rotateCommittee, rotate:committee removed). Committee lock stays the signer's lock in bootstrap until an always-success script is deployed (T11).
gotchas: do NOT merge freeze/v1-contracts to main before testnet is redeployed as v3 (T11): the SDK now decodes only v2 formats while testnet runs v1 (website is unaffected: it reads mirror JSON prices, not blobs). Devnet e2e not yet run on v2: needs deploy:code on devnet with v2 binaries and rebuilt lean-oracle-publisher:dev / lean-oracle-mirror:dev images. The "backup re-proposes" test covers one role layout picked by header hashes; a test that forces the backup to hold all observations would pin the fix independent of hashes.
next: T9 (run devnet e2e on v2: deploy:code to devnet, docker build images, LEAN_DEVNET=1 node --test apps/deploy/tests/; then add the CI job), T10 docs (oracle-design.md sections 3/5/6/7/9, apps/web content formats/governance/threat model, TPOU->LOPU), T11 (deploy an always-success lock script, bootstrap committees with it, testnet v3 redeploy, SDK major version). Then /review and /ship.

## T9: devnet e2e on v2 + CI job
status: done locally; CI job unvalidated
did: deploy:code devnet (v2 priceFeedType 0xb9f5..., publisherSetType 0x3eb4..., deployer = offckb genesis #0 passed as DEVNET_DEPLOYER_PRIVATE_KEY for the command only); docker build lean-oracle-{publisher,mirror}:dev; LEAN_DEVNET=1 devnet.e2e 5/5, onboarding.e2e 3/3. Added sendWhenMature retry (routine rotation waits ~90 s for median time even at a 1 s interval), --add-host host-gateway on test containers, .github/workflows/devnet-e2e.yml.
files: apps/deploy/tests/*.mjs, deployments/devnet.json, .github/workflows/devnet-e2e.yml
gotchas: the workflow has never run on GitHub; its offckb RPC-listen patch (find ckb.toml under $HOME, sed listen_address to 0.0.0.0:8114, restart) is untested on Linux. First push of the branch will tell. apps/deploy/.env has no DEVNET deployer key; use the genesis key inline for devnet.
next: T10 docs, T11 always-success lock + testnet v3, then push branch (ask user) so CI + devnet-e2e run, then /review and /ship.

## T10: docs
status: done
did: updated docs/oracle-design.md (status line, principle 3, new 3.2 Governance, sections 5-10, decision log), apps/publisher/README.md + apps/deploy/README.md (governance commands), website pages (operate/governance runbook, learn/trust-model, reference formats/rust/errors/sdk, build/tutorial, learn costs/mirror/price-data, operate/index), SDK CHANGELOG 2.0.0 (unreleased) and README. No stale TPOU/125-byte/rotate:committee/signRotation/50 MB mentions remain outside history. apps/web builds (62 pages).
files: see commit after f3b4655
gotchas: storage figures in docs are the testnet measurement (0.4 GB/day per 10-feed committee store); update them if the P0 retention task lands.
next: T11 (always-success lock for committee cells + testnet v3 redeploy; needs user approval: spends testnet CKB and replaces the live testnet), push branch (ask), then /review and /ship.
