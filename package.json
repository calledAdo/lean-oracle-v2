{
  "name": "threshold-price-oracle-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w lean-oracle-sdk && npm run build -w lean-oracle-publisher && npm run build -w lean-oracle-mirror",
    "test": "npm test -w lean-oracle-sdk && npm test -w lean-oracle-publisher && npm test -w lean-oracle-mirror"
  },
  "engines": {
    "node": ">=22"
  }
}
