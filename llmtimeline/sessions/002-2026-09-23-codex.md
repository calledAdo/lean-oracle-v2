# Session 002 · 2026-09-23 · codex

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Current-set-only contract migration
status: done
intent: remove previous publisher sets and activation times from Rust contracts, with immediate rotation and current-only attestation authorization.
did: changed PublisherSet encoding to version 2 with only current set; removed retirement operation; made rotation advance set index and governance nonce exactly one and retain old-set authorization plus all-new-key proof of possession; made PriceAttestation creation use only the active set index. Updated Rust unit tests for the new layout and rejection of old version/trailing bytes.
files: contracts/common/src/publisher_set.rs, contracts/publisher_set_type/src/main.rs, contracts/price_attestation_type/src/main.rs, llmtimeline/state.md, llmtimeline/sessions/002-2026-09-23-codex.md
decisions: removed activation time to make rotation immediate, following the agreed Lean Oracle current-set-only policy. Kept the rotation opcode and domain-separated update hash for the witness protocol.
gotchas: TypeScript code and local devnet deployment artifacts still use version 1; they are intentionally outside this task and cannot interoperate with the new contract layout. No CKB-VM integration tests exist in this repository.
next: update TypeScript codec/services/deployment and rebootstrap devnet state before running an end-to-end chain test.

## Verification
status: done
intent: confirm the changed contracts compile and the layout/selection tests pass.
did: ran host-target cargo test (2 passed) and RISC-V release cargo build (passed) with the configured cross compiler.
files: ignored target/ build output
next: after TypeScript is migrated, add shared golden vectors and run devnet transaction tests.
