{
  "name": "lean-oracle-mirror",
  "version": "1.0.0-alpha.0",
  "private": true,
  "description": "Lean Oracle mirror: ingests finalized updates from publishers, verifies them against the committee cell, stores full history and serves a public read API. Distributed as a Docker image.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "npm run build && node --test tests/"
  },
  "dependencies": {
    "lean-oracle-sdk": "*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.13"
  }
}
