# Session 003 · 2026-09-23 · codex

<!-- Append one block per task as you finish or stop it. Never edit blocks above. -->

## Derived publisher quorum
status: done
intent: allow one publisher and derive quorum mathematically without configurable minimum threshold or publisher count fields.
did: removed the stored threshold and both minimum fields from PublisherSet encoding, bumped the layout to version 3, derived quorum as floor(2n/3)+1 in Rust signature verification, and removed floor continuity checks from rotation. Preserved governance nonce, flags, and the existing maximum of nine publishers. Added a test for all supported n=1..9 and empty/oversized rejection.
files: contracts/common/src/publisher_set.rs, contracts/common/src/signatures.rs, contracts/publisher_set_type/src/main.rs, llmtimeline/state.md, llmtimeline/sessions/003-2026-09-23-codex.md
decisions: n=1 is intentionally valid with quorum 1; n=2 and n=3 require all signers; n>=4 follows strict two-thirds quorum. Kept the existing publisher maximum rather than adding a minimum.
gotchas: TypeScript and local devnet artifacts still target the old layout. GOVERNANCE_LOCKED's majority check is redundant under the derived quorum, though its non-removable flag behavior remains.
next: migrate TypeScript codecs and callers to version 3 when that work is authorized.

## Verification
status: done
intent: confirm the modified contracts build and derived quorum values match the intended formula.
did: Rust host tests passed (3); RISC-V release build passed.
files: ignored target/ build output
next: run cross-language golden vectors and devnet integration after TypeScript migration.
