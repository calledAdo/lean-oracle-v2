> llmtimeline · cross-agent work record. state.md is the live snapshot — rewrite it in place. sessions/ is append-only history — never edit past files. Any agent: read this file and the newest sessions/ entries before starting.

# Project State — updated 2026-09-26T18:30Z by opus (session 008)

## Goal
Build the pull-based threshold price oracle for CKB specified in docs/oracle-design.md (final 2026-09-24, first version — nothing deployed before it): committees of permissioned publishers sign per-tick Merkle-batched price updates, a mirror API serves them, and projects forward-update their own Type ID-unique price_feed_type cells.

## Tasks
- [x] Review current architecture and prelaunch readiness using gstack perspectives (session 008 opus): docs/review/2026-09-26-architecture-and-gstack-plan.md.
- [ ] P0: testnet storage retention (~1.3 GB/day, no pruning in publisher or mirror) + watchdog disk-free alert; fix the 50 MB/day figure in docs/oracle-design.md §8.
- [ ] gstack setup: CLAUDE.md (commands, contract rules, routing), backlog choice (TODOS.md vs llmtimeline), versioning for /ship, branch+PR flow, /setup-deploy for the droplet.
- [x] /office-hours: design doc docs/designs/v1-contract-freeze.md APPROVED (approach B, 7 freeze items, mainnet gate n>=4).
- [x] /plan-eng-review on docs/designs/v1-contract-freeze.md: 20 decisions (D1-D20) + Codex outside voice; 11 implementation tasks T1-T11 listed in the doc.
- [~] Implement contract freeze T1-T11 (on branch freeze/v1-contracts: T1-T8 done, checksums recorded, all tests green; T9 done locally (CI job unrun), remaining T10 docs, T11 always-success lock + testnet v3; do not merge before T11) (order: T1 formats first; then contracts T2/T3 in parallel with vectors T5 -> SDK T6 -> publisher T7 + mirror T8; then deploy T11 + CI T9; docs T10). Use /review then /ship per PR.
- [ ] Backlog: TWAP60 windowed feeds (off-chain; apps/publisher/src/methodology.ts + config templates; design item 6).
- [ ] Backlog: move Crypto.CKB/USDT and Crypto.CKB/USDC into the majors committee config and retire the ckb committee (config-only, after the freeze).
- [ ] ckb-testtool suite for publisher_set_type (negative rotation cases); consider running devnet e2e in CI.
- [x] Finalize the design in docs/oracle-design.md.
- [x] Contracts: common (LEAN tags, PublisherSet v1 layout, update header/leaf codecs, Merkle, verify_price_update, feed cell data), price_feed_type, publisher_set_type; ckb-testtool tests.
- [x] Remove all prototype code (boundary attestations, Binance signer/coordinator/relayer, deployment tooling, Docker/env/secrets, old docs, TOTE tags, @tote packages).
- [x] SDK design (docs/sdk-design.md); rename to `lean-oracle-sdk` (packages/sdk) and Rust crate `lean-oracle-common`.
- [x] Shared vectors: vectors/protocol.json generated by tests/src/vectors.rs.
- [x] SDK root, `/protocol`, `/publisher` (9 tests pass, byte-identical to Rust incl. signatures).
- [x] SDK `/presets`, `/ckb`, `/tx` (feed create/update/burn, committee bootstrap/rotate).
- [x] SDK `/mirror` (with the mirror API). (board fixed in session 008: packages/sdk/src/mirror exists)
- [x] Deployment tooling apps/deploy (deploy-code, bootstrap/rotate/show); contracts deployed on local offckb devnet (deployments/devnet.json).
- [x] Publisher reads committee cell from CKB (get_cells), exits on rotation/pause.
- [x] Devnet end-to-end test (apps/deploy/tests, LEAN_DEVNET=1): 5/5 pass on a real node.
- [x] SDK off-chain formats: Observation (binary, TPOB) and CommitteeConfig (canonical JSON, quorum-approved).
- [x] Publisher service apps/publisher (TypeScript, Docker-only): recorder + 9 venue adapters, methodology, integer EMA, rotating leader, signer with SQLite double-sign guard, signed WS peer envelopes, sync, HTTP API, CLI, Dockerfile. 10 tests pass; 4-process local committee verified.
- [ ] Publisher follow-ups: verify venue parsers against live traffic; KuCoin/MEXC adapters; config-version switching at activation ticks; KMS keys; multi-arch image publishing to GHCR.
- [x] Mirror API (apps/mirror, live on testnet; board fixed in session 008).
- [ ] Pools repo (asset-up-down-pools): keeper-owned lane feed cell, exact-tick settlement; it currently still depends on the removed @tote/oracle-sdk and price_attestation.
- [x] Install the `office-hours` Codex skill from `garrytan/gstack`.
- [x] Install the `plan-ceo-review` Codex skill from `garrytan/gstack`.
- [x] Install the `review` Codex skill from `garrytan/gstack`.
- [x] Install the `qa` Codex skill from `garrytan/gstack`.
- [x] Install the `ship` Codex skill from `garrytan/gstack`.
- [x] Install the `cso` Codex skill from `garrytan/gstack`.

## Summary
Rust contracts and tests pass (price_feed_type, publisher_set_type, lean-oracle-common, vectors). SDK `lean-oracle-sdk` (packages/sdk): root, /protocol (all on-chain codecs + Observation + CommitteeConfig), /publisher (signing); 11 tests. Publisher `lean-oracle-publisher` (apps/publisher, private, Docker-only; operator config now `key` + `committee.configDir`): 16 tests incl. in-process committee scenarios (identical verifiable updates, down leader, lagging publisher sync, outlier refuses, double-sign guard, n=1); a real 4-process committee over WebSockets with mock sources finalized 8 ticks in ~9 s, verified by the SDK. Committee templates: apps/publisher/configs/{majors,ckb}.template.json. Docker image lean-oracle-publisher:dev builds (244 MB); a 4-container committee on a Docker network finalized and served updates that verify with the SDK, and a restarted container resynced from its persisted volume. Spec updated with the implemented formats (observation layout, canonical config JSON + signature list, integer EMA, lower medians, peer envelopes).
The public `office-hours` Codex skill from `garrytan/gstack` is installed at `/Users/adokiye/.codex/skills/office-hours` using the official skill installer. Its manifest and supporting sections are present.
The current session is installing the public `plan-ceo-review` Codex skill from the same repository.
The public `plan-ceo-review` Codex skill is installed at `/Users/adokiye/.codex/skills/plan-ceo-review`; its manifest and supporting sections are present.
The current session is installing the public `review` Codex skill from the same repository.
The public `review` Codex skill is installed at `/Users/adokiye/.codex/skills/review`; its manifest, checklists, and specialist sections are present.
The current session is installing the public `qa` Codex skill from the same repository.
The public `qa` Codex skill is installed at `/Users/adokiye/.codex/skills/qa`; its manifest, references, sections, and report template are present.
The current session is installing the public `ship` Codex skill from the same repository.
The public `ship` Codex skill is installed at `/Users/adokiye/.codex/skills/ship`; its manifest and workflow sections are present.
The current session is installing the public `cso` Codex skill from the same repository.
The public `cso` Codex skill is installed at `/Users/adokiye/.codex/skills/cso`; its manifest, acknowledgements, and audit phase section are present.

## Next
Session 008 ran /office-hours: approved design docs/designs/v1-contract-freeze.md (open infrastructure; reference cases pools settlement + CKB lending; approach B contract freeze). Then /plan-eng-review locked it (see the Engineering review section and GSTACK REVIEW REPORT at the end of that doc). Next: implement T1 (contracts/common formats) on a branch, then the lanes listed under 'Worktree parallelization strategy'.
Session 008 review is done: read docs/review/2026-09-26-architecture-and-gstack-plan.md (findings ranked P0–P2 and a gstack workflow). Recommended next: (1) P0 retention + disk alert on the droplet, which fills in ~1–2 weeks at the current rate; (2) gstack repo setup; (3) planning reviews to freeze the v1 contracts before any mainnet work.
Testnet is live (v2 contracts, one publisher, mirror at https://64-227-40-35.sslip.io, watchdog + backups on the droplet); lean-oracle-sdk 1.0.0 is on npm; the landing page (apps/web) is done with the 卜 logo. See sessions/004 and 005. 
Current work: no pending action for this session. Documentation at /docs in apps/web is built (Fumadocs, content/docs) and published to GitHub Pages by .github/workflows/pages.yml (https://calledado.github.io/lean-oracle-v2/). Publisher onboarding tooling (shadow mode, rotation CLI) is done and drilled on devnet. Deferred by the user: multi-publisher onboarding (step 4), mainnet prep (step 5). The pools repo is out of scope.

## Notes
- Build: `CC_riscv64imac_unknown_none_elf=riscv64-elf-gcc cargo build --release`; test: `cargo test --workspace --target aarch64-apple-darwin` (tests load the RISC-V binaries, so build first). Rust pinned to 1.92 (1.98 emits unsupported atomics).
- tests/ pins ckb-testtool =1.0.1 and the ckb 1.1.0 crate family (newer ckb crates need rustc 1.95), same as lean-oracle; it is excluded from workspace default-members.
- All contracts use hash_type data2, immutable, no upgrade key.
- This machine's ISP DNS resolvers (2c0f:f5c0:…, 10.116.10.22) do not answer for exchange domains, and Kraken/OKX/KuCoin IPs are blocked; use `network.dns: { mode: "doh" }` / probe `--doh`. Sandboxed shells have no DNS at all — live network checks need dangerouslyDisableSandbox.
- Docker CLI works on this machine (symlink fixed by the user on 2026-09-24; Docker Desktop 4.91, engine 29.8, arm64).
- Devnet e2e: offckb devnet on 127.0.0.1:8114, `deploy-code` done, image lean-oracle-publisher:dev built, then `LEAN_DEVNET=1 node --test apps/deploy/tests/`. Uses offckb genesis keys #0 (deployer) and #1 (project).
- JS tests: `npm test` at repo root (SDK then publisher). Local committee: apps/publisher/README.md.
- SDK tests: `npm test` (builds, then node --test packages/sdk/tests). Regenerate vectors: `LEAN_WRITE_VECTORS=1 cargo test -p tests --target aarch64-apple-darwin vectors`.
- A pre-cleanup snapshot of the repo was saved in session 004's scratchpad (temporary; not durable).
